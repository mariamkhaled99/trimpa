"use client";
import React from 'react';
import SeatContainer from '@/components/seats/seatContainer';

const Page = () => {
  return <><SeatContainer       overflow="hidden" // Prevent scrolling on the whole page
  /></>;
};

export default Page;
